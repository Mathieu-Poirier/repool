- Date validation X
- Date uniqueness
- make sure hours can't be more than 24h in 24h mode maybe with regex
- Time uniqueness
- Add id field
- Repeat schedule validation
- Split to consumer and producer process
- Have consumer iterate through the file and parse each line correctly then replace the next_run field with the correct time and date according to the repeat schedule
- Have producer write to a known file in a serialized format
- Create fifo file with mkfifo for singals (parse current jobs, remove job by id (autoincrement))
- flock the jobs file (manage lock)
- implement std::system or spawn in a non blocking way
- clean up code and repo